## 针对规范修订版 2.1 的 20 项更新

## 上一章

上一章描述了 PCI Express 热插拔模型。同时，针对所有支持热插拔功能的设备和外形规格，也定义了一套标准使用模型。对于热插拔卡而言，功耗同样是一个关键问题。在系统运行期间添加新卡时，必须确保其功耗需求不超过系统所能提供的上限。因此，需要一种机制，在允许设备运行前查询其功耗需求。功耗预算寄存器正是为此而设。

## 本章

本章描述了规范 2.1 修订版中新增的变更与特性。其中部分主题（如与电源管理相关的内容）已在其他章节中阐述，但其余主题在逻辑上并无更合适的归属。最终，将这些内容集中整合至同一章节，既能确保全面覆盖，也有助于明确哪些是新特性。

## 下一章

下一节是本书附录，涵盖以下主题：使用力科工具调试 PCI Express 流量、PCI Express 架构的市场与应用、利用 PCI Express 技术实现智能适配器与多主机系统、锁定功能的传统支持以及本书术语表。

## PCIe 规范 2.1 修订版变更

PCIe 规范 2.1 修订版引入了几项变更，旨在提升性能或改善运行特性。该版本未增加新的数据速率，因此被视为增量修订。这些修改大致可分为四个改进领域：系统冗余、性能、电源管理和配置。

## 系统冗余改进：多播

多播能力允许一个已发布的写 TLP 同时路由到多个目的地，从而实现自动复制数据冗余副本或支持多头图形等功能。如第 888 页图 20-1 所示，源自一个端点的 TLP 可以仅根据其地址路由到多个目的地。在此示例中，数据被发送到视频端口进行显示，同时其冗余副本会自动路由到存储设备。当然，还有其他方式可以支持此功能，但这种方式在链路使用方面非常高效，因为它不需要接收方将数据包重新发送到次要位置。

图1：图20-1：多播系统示例

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-002.jpg)

此机制仅支持已发布的、基于地址路由的请求（例如内存写入），这些请求包含待传输的数据以及一个可解码以指示哪些端口应接收该数据的地址。非已发布的请求即使其地址落在多播地址范围内，也不会被视为多播请求。这些请求将像往常一样作为单播 TLP 进行处理。

多播操作的设置涉及为每个将参与的路由元件和功能编程一个新的寄存器块，称为多播能力结构。该块的内容如图 20-2（第 889 页）所示，其中可以看到它们定义了地址以及 MCG（多播组编号），这些编号说明了功能是否应发送或接收传入 TLP 的副本，或者端口是否应转发它们。接下来

## 第20章：规范修订版2.1的更新

我们将描述这些寄存器，并讨论它们如何在系统中用于创建多播操作。

图2：图20-2：多播能力寄存器

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-003.jpg)

## 多播能力寄存器

图中顶部的能力头寄存器包含能力 ID 0012h、4 位版本号，以及指向寄存器链表中下一个能力结构的指针。

## 多播能力

该寄存器（详见第 890 页图 20-3）包含多个字段。其中 MC_Max_Group 值定义了该功能所设计支持的多播组数量减一，因此值为零时表示支持一个多播组。
支持组功能。窗口大小请求仅对端点有效，在交换机和根端口中保留，表示为此目的所需的地址大小（以2的幂表示）。

图3：图20-3：多播能力寄存器

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-004.jpg)

最后，第 15 位指示该功能是否支持在转发 TLP 时因地址变更而重新生成 ECRC 值。有关此功能的更多详细信息，请参阅第 895 页的“覆盖示例”章节。

## 多播控制

该寄存器（见第 890 页图 20-4）包含 MC_Num_Group 字段，该字段由软件编程配置本功能所使用的多播组数量。默认值为零，规范指出，若此处编程值超过 MC_Max_Group 寄存器定义的最大值，将导致未定义行为。MC_Enable 位用于启用该组件的多播机制。

图4：图20-4：多播控制寄存器

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-004-1778244554262-1.jpg)

## 第20章：规范修订版2.1的更新内容

## 多播基地址

基地址寄存器（如图 20-5 所示，第 891 页）包含该组件多播地址范围的 64 位起始地址。多播索引位置寄存器指示地址中用于定位多播组（MCG）编号的比特位位置。当传入 TLP 的地址落在以该基地址为起点的多播地址范围内时，逻辑电路将根据索引位置寄存器指定的比特位数偏移地址，并将后续比特位（最多 6 位，支持最多 64 个组）解析为该 TLP 的 MCG 编号。该 MCG 编号将决定端口是否需要转发此 TLP 的副本。

图5：图20-5：多播基地址寄存器

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-005.jpg)

图 20-6（第 892 页）展示了在地址中定位 MCG 的示例。此处索引位置值为 24，因此 MCG 位于地址的第 25 至 30 比特位。值得注意的是，由于基地址未定义地址的低 12 位，多播索引位置必须大于等于 12 才有效。若该值小于 12 且 MC_Enable 位被置位，则组件的行为将处于未定义状态。

图6：图20-6：多播组编号位置

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-006.jpg)

## 多播接收

该 64 位寄存器是一个位向量，用于指示此功能应接受副本或此端口应转发副本的 64 个多播组（MCG）。例如，若 MCG 值为 47，且该寄存器中第 47 位被置位，则此功能应接收该副本，或此端口应转发该副本。

## 多播全部阻止

这个 64 位寄存器指示端点功能被阻止发送哪些 MCG，以及交换机或根端口被阻止转发哪些 MCG。例如，可以在交换机或根端口中对其进行编程，以防止其将多播 TLP 转发给无法理解这些 TLP 的端点。被阻止的 TLP 被视为错误条件，下一节将描述如何处理该错误。

## MC 阻止未翻译

这个 64 位寄存器的含义和用途与“全部阻止”寄存器几乎相同，但它不适用于其 AT 头部字段显示为已翻译的 TLP。此机制可用于设置一个受保护的多播窗口，该窗口只能接收已翻译的地址。

如果由于这两个阻止寄存器中任一寄存器的设置而导致 TLP 被阻止，则该 TLP 将被视为 MC 阻止的 TLP，这意味着它会被丢弃，并且端口

## 第20章：规范修订版2.1的更新

或由功能记录此错误并将其作为错误信号发出。记录错误时，需在其状态寄存器或辅助状态寄存器中设置相应的"目标终止信号"位。不过这些信息仅能提供有限帮助，因此规范强烈建议在具备多播能力的功能中实现高级错误报告（AER）寄存器，以便于隔离和诊断故障。

规范指出，所有实现多播能力寄存器的功能都必须包含该寄存器，但如果端点功能未实现地址转换服务（ATS）寄存器，设计人员可选择将这些位设为保留位。

## 多播示例

此时，一个示例将有助于说明如何使用这些寄存器来设置多播环境。为此，我们首先为相关寄存器赋予一些值：

- MC_Base_Address = 2GB（多播范围的起始地址）
- MC_Max_Group = 7（表示此设计支持 8 个窗口）
- MC_Window_Size_Requested （表示端点请求了 即 1KB 的大小）
- MC_Index_Position = 12（表示每个窗口的实际大小为 ）
- MC_Num_Group = 5（表示软件仅配置了可用多播窗口中的 6 个）

基于这些寄存器设置，第 894 页图 20-7 中的图像展示了结果。多播窗口范围起始于 2 GB，最高可达 （窗口大小）。然而，软件仅启用了 6 个窗口，因此实际多播地址范围为 2 GB 至 。所有窗口大小相同，并与 MCG 对应：MCG 0 为第一个窗口，MCG 1 为下一个窗口，以此类推。

图 7：图 20-7：多播地址示例

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-008.jpg)

## MC 覆盖 BAR

最后一组寄存器是实现了多播功能的交换机和根端口所必需的，但端点设备并未实现这些寄存器。该 BAR 的设计动机在于支持两种特殊情况。首先，即使端点设备并非为多播而设计，端口仍可在多播窗口命中时向下游转发 TLP。其次，端口可将多播 TLP 向上游转发至系统内存。这两种情况均通过将请求地址的部分内容替换为目标可识别的地址来实现。这使得组件中的单个 BAR 即使未设计多播功能，也能同时作为单播和多播写入的目标。

如图 20-8（第 895 页）所示，该寄存器块包含一个将叠加到传出 TLP 上的地址，以及一个 6 位的叠加大小指示符。此处所指的大小，是指原始 64 位地址中将被保留的位数，其余所有位将被叠加 BAR 位替换。规范中至少有一处错误地将其称为字节大小，但在其他位置已明确说明这是一个位数。请注意，叠加大小值必须为 6 或更高才能启用叠加操作。若指定的大小为 5 或更低，则不会发生叠加，地址保持不变。

## 第 20 章：规范修订版 2.1 的更新

图 8：图 20-8：多播叠加 BAR

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-009.jpg)

## 叠加示例

现在考虑需要地址覆盖的情况，如图 20-9（第 896 页）所示。此时待转发的 TLP 地址 ABCD_BEEFh 落在定义的多播范围内（也称为多播命中），且出口端口已在覆盖 BAR 中配置了有效值。

覆盖情况会导致先前在多播能力寄存器描述中提到的 ECRC 值出现异常。如果被覆盖修改地址的 TLP 包含 ECRC，该值将因地址变更而失效。交换机和根端口可选择基于新地址重新生成 ECRC，使其继续发挥校验作用。若路由代理不支持此功能，则直接丢弃 ECRC，并将 TD 报头位强制清零以避免混淆。

ECRC 再生可能引发潜在问题。若传入的 TLP 原本存在错误，但因地址被修改而重新生成 ECRC 值，则会无意中掩盖原始错误。为避免此情况，路由代理必须首先验证原始 ECRC。若检测到错误，则必须在传出 TLP 上强制生成错误 ECRC，方法是在附加前反转计算出的 ECRC 值，以确保目标端能识别该错误状态。

图 9：图 20-9：覆盖示例

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-010.jpg)

## 路由多播 TLP

当交换机或根端口检测到 MC 命中（地址落在 MC 范围内）时，正常路由被暂停。从地址中提取 MCG 并与所有端口的 MC_Receive 寄存器进行比较，以确定哪些端口应转发此 TLP 的副本。对应接收寄存器位已设置的端口将转发 TLP 副本，除非其对应的 MC 阻塞寄存器位也已设置。若没有端口转发该 TLP 且没有功能消耗它，则静默丢弃。为防止环路，TLP 绝不会从其入口端口被转发回去（ACS 情况可能例外）。

端点提取 MCG 并与接收寄存器进行比较。若不匹配，TLP 将被静默丢弃。若端点不支持多播功能，则会将 TLP 视为普通地址处理。

## 第20章：规范修订版2.1更新

## 拥塞避免

多播技术的应用将使系统流量按多播流量比例增加，从而带来数据包拥塞风险。为避免产生背压，多播目标应设计为能够"全速"接收多播流量，即实现最小延迟。为防止链路过载，多播发起方应限制数据包注入速率。系统设计者需谨慎选择组件以应对此问题，例如选用缓冲区容量足以处理预期流量的交换机和根端口，以及能够快速接收传入多播数据包以避免故障的端点设备。

## 性能改进

系统性能通过新增四项功能得到提升：

1. AtomicOps 替代传统事务锁定机制
2. TLP 处理提示，允许软件建议缓存选项
3. 基于 ID 的排序以避免不必要的延迟
4. 替代路由 ID 解释以增加设备中可用的功能数量

## 原子操作

共享资源或相互通信的处理器有时需要无中断的（即“原子性”）访问系统资源，以执行诸如测试和设置信号量等操作。在并行处理器总线上，这是通过断言锁定引脚来锁定总线实现的，直到发起方完成整个序列（先读取后写入），在此期间其他处理器不允许在总线上发起事务。PCI 包含一个锁定引脚，用于在 PCI 总线上应用与处理器总线相同的模型，从而使该协议能够与外围设备一起使用。

该模型在共享处理器总线上运行缓慢，在接入 PCI 总线时性能更差。这也是 PCIe 将其使用范围限制在传统设备上的原因之一。然而，随着当今 PC 中共享处理技术的日益普及（如图形协处理器和计算加速器），这一问题再次凸显——因为不同的计算引擎需要能够共享原子协议。PCIe 解决该问题的方式是引入三个新命令，每个命令都能在目标设备内原子性地执行一系列操作，

## PCI Express 技术

而无需在接口上执行一系列不可中断的独立命令。这些被称为 AtomicOps 的新命令包括：

1. FetchAdd（读取并相加）——该请求包含一个"加数"值。它会读取目标位置，将"加数"值与之相加，将结果存储回目标位置，并返回目标位置的原始值。该功能可用于支持原子性地更新统计计数器。
2. 交换（无条件交换）——此请求包含一个“交换”值。它会读取目标位置，将“交换”值写入其中，并返回原始目标值。这对于原子性地读取和清除计数器可能很有用。
3. CAS（比较并交换）——此请求包含一个“比较”值和一个“交换”值。它会读取目标位置，将其与“比较”值进行对比，如果两者相等，则写入“交换”值。最后，它会返回目标位置的原始值。这可以作为管理信号量的“测试并设置”机制使用。

端点（Endpoint）和根端口（Root Port）均可选择性地作为原子操作请求者（AtomicOp Requester）和完成者（Completer），这或许看似出人意料——因为在个人电脑（PC）中，此类事务通常仅由中央处理器发起。但现代系统可能包含作为协处理器运行的端点，此时它需要能够使用原子操作（AtomicOps）来正确处理协议。所有三种命令均支持 32 位和 64 位操作数，而比较并交换（CAS）还额外支持 128 位操作数。实际使用的操作数大小将在报头的长度字段（Length field）中给出。具备点对点访问能力的路由元件（如交换端口和根端口）需要支持原子操作路由能力，才能识别并转发这些请求。

一个自然产生的问题是：系统（根复合体）将如何根据处理器活动被指示生成这些新命令，因为可能不存在直接对应的处理器总线命令。规范提出了两种方法。第一种，根复合体可被设计为识别特定的处理器活动，并将其解释为“导出”PCIe 原子操作作为响应。第二种，可采用类似于传统配置访问的基于寄存器的方法。在这种情况下，一个寄存器可能提供目标地址，而另一个寄存器指定应生成的命令，两者结合即可生成请求。

AtomicOp Completers 可通过设备能力 2 寄存器中新增的三个位来识别，如图 20-10（第 899 页）所示。该寄存器的位 6 还用于标识路由元素是否支持路由 AtomicOps。

## 第20章：规范修订版2.1的更新

传统的 PCI 当然不支持原子操作（AtomicOps），也没有直接的方法将其转换为 PCI 命令。因此，PCIe 到 PCI 桥接器不支持原子操作。如果该总线上需要原子访问，则必须使用传统的锁定协议来实现，并且规范指出锁定事务和原子操作可以在同一平台上同时运行。

图10：图20-10：设备能力2寄存器

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-013.jpg)

## TPH（TLP 处理提示）

添加关于系统应如何处理针对内存空间的 TLP 的提示，可以改善延迟和流量拥塞。规范将这种特殊处理描述为：提供关于系统中多个可能的缓存位置中，哪个是临时副本的最佳位置的信息。
TLP 的一部分。规范指出，由于 TPH 所描述的用途与缓存相关，因此通常不建议将其用于针对不可预取内存空间的 TLP。如果确实需要这种用法，则必须确保缓存此类 TLP 不会导致不良副作用。

## TPH 示例

设备写入，主机读取。为帮助阐明 TPH 的动机，请考虑第 901 页图 20-11 所示的示例。在此示例中，端点将数据写入内存，供 CPU 后续使用。具体流程如下：

1. 首先，端点发送一个内存写入 TLP，其中包含映射到系统内存的地址。该数据包被路由至根复合体（RC）。
2. RC 将此识别为对可缓存内存空间的访问，并在窥探 CPU 缓存时暂停其进程。这可能导致 CPU 在事务继续之前执行写回周期以更新系统内存，如步骤 2a 所示。
3. 一旦任何写回操作完成，RC 便允许写入操作更新系统内存。
4. 在某个时刻，端点向 CPU 通知数据已交付。
5. 最后，CPU 从内存中获取数据以完成该序列。

## 第20章：规范修订版2.1的更新

图 11：图 20-11：TPH 示例

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-015.jpg)

该序列虽然可行，但通过在系统中增加中间缓存可提升性能。为说明这一点，请参考第902页图20-12所示的示例。从端点的角度来看，操作流程相同，但系统处理方式有所不同。具体步骤如下：

1. 端点执行相同的内存写入操作，但此次包含 TPH 位。该写入操作仍像之前一样由交换机转发至根复合体。
2. RC 理解到，这次内存访问必须像之前一样被嗅探到 CPU。然而，一旦嗅探处理完成，RC 通过 TPH 位获知，应将此 TLP 存储到中间缓存中，而非直接写入系统内存。
3. 端点通知 CPU 数据项已送达。
4. CPU 从指定地址读取数据，但此时数据已在中间缓存中找到，因此请求无需访问系统内存。这带来了缓存设计的常规优势：更快的访问速度以及减少系统内存的流量。

这是一个简单的设备写入到主机读取（DWHR）示例，用于说明这一概念。但不难想象，在一个拓扑结构更复杂的系统中，可以在交换机或其他位置放置更多缓存，从而为其他目标实现同样的优势。

图 12：图 20-12：使用系统缓存的 TPH 示例

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-016.jpg)

主机写入到设备读取。为了说明反向概念（称为主机写入到设备读取，即 HWDR），请参考第 903 页图 20-13 所示的示例。在此示例中，CPU 在第一步发起一次内存写入操作，其地址指向 PCIe 端点。该数据包包含 TPH 位，告知 RC 应将其存储在目标设备附近的中间缓存中，而非前一个示例中 RC 所使用的缓存。在此场景下，交换机内置的缓存可满足此需求。随后，TLP 在第二步被转发至目标端点。当数据更新频率低但端点频繁读取时，此模型尤为有利。这使得原本需访问系统内存的多次内存读取操作可由缓存处理，从而减轻从交换机到 RC 的链路以及内存访问路径的负载。

## 第20章：规范修订版2.1的更新内容

图 13：图 20-13：面向端点的 TLP 的 TPH 使用方式

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-017.jpg)

设备到设备。最后一个示例见第 904 页图 20-14，其中两个端点通过由 TPH 位引导至中间缓存的共享内存位置进行通信（称为设备读/写至设备读/写，即 D*D*）。在此场景中，两者可能更新各自需要以"主读"方式处理的不同位置，或一个端点更新另一端点需多次读取的数据。两种情况下，使用中间缓存均可提升系统性能。

图 14：图 20-14：端点间的 TPH 使用

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-018.jpg)

## TPH 头部位

TLP 头部中的若干位描述了提示信息的使用方式。首先，如第 905 页图 20-15 顶部中间所示，TH（TLP 提示）位指示该 TLP 是否使用了可选的 TPH 位。当该位置位时，PH（处理提示）位表示下一级信息。

## 第20章：规范修订版2.1的更新

图 15：图 20-15：TPH 头部比特位

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-019.jpg)

当 TH 比特位被置位时，PH 比特位（如图 20-15 第 905 页右下角所示）将取代地址字段中原本保留的两个最低有效位（LSBs）。对于 32 位地址，这些比特位位于字节 11[1:0]；而对于所示的 64 位地址，则位于字节 15[1:0]。其编码方式在第 905 页的表 20-1 中进行了描述。这些提示由请求方根据对当前使用数据模式的了解提供，而完成方自身很难推导出此类信息。

表 1：表 20-1：PH 编码表

| PH [1:0] | 处理提示           | 使用模型                                                     |
| -------- | ------------------ | ------------------------------------------------------------ |
| 00b      | 双向数据结构       | 表示主机和设备频繁进行读写访问。                             |
| 01b      | 请求者             | D*D*（设备到设备传输）。表示设备频繁进行读写访问。星号表示任一设备可能正在进行读取或写入操作。 |
| 10b      | 目标               | DWHR、HWDR（设备到主机或主机到设备传输）。表示主机频繁进行读写访问。 |
| 11b      | 带优先级的访问目标 | 与目标相同，但额外包含时间性重用优先级信息。表示主机频繁读写访问，且访问数据具有较高的时间局部性。 |

下一级信息是导向标签字节，提供系统特定的信息，指示缓存此 TLP 的最佳位置。有趣的是，该字节在头部中的位置取决于请求类型。对于发布的内存写入，标签字段被重新用作导向标签（不会返回完成，因此不需要标签）；而对于内存读取，两个字节使能字段被重新用于此目的（预取读取不需要字节使能）。这些位的含义是具体实现相关的，但它们需要唯一标识系统中所需缓存的位置。

规范中描述了 TPH 的两种格式，这种级别的提示信息（TH + PH + 8 位转向标签）被称为基线 TPH，是第一种格式，也是所有提供 TPH 的请求所必需的。第二种格式使用 TLP 前缀来扩展转向标签（更多详情请参见第 908 页的“TLP 前缀”）。

## 转向标签

这些值由软件编程到表中，以便在正常操作期间使用。规范建议将该表置于 TPH 请求者能力结构中（如图 20-16 所示，第 906 页），但也可以将其内置于 MSI-X 表中。对于给定的功能，只能使用这两个表位置中的一个。该位置由请求者能力寄存器（如图 20-17 所示，第 907 页）的 ST 表位置字段[10:9]指定。这两个位的编码如表 20-2 所示（第 907 页）。

表 2：图 20-16：TPH 请求者能力结构

| 31                                          | 15           | 7                         |      |
| ------------------------------------------- | ------------ | ------------------------- | ---- |
| PCI Express 能力寄存器                      | 下一能力指针 | PCI Express 能力 ID (17h) | DW0  |
| TPH 请求者能力寄存器                        | DW1          |                           |      |
| TPH 请求者控制寄存器                        | DW2          |                           |      |
| TPH ST 表（可选）（大小由 ST 条目数量决定） | DW3 - •      |                           |      |

## 第20章：规范修订版2.1的更新内容

图 16：图 20-17：TPH 能力与控制寄存器

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-021.jpg)

表 3：表 20-2：ST 表位置编码

| 位[10:9] | ST 表位置            |
| -------- | -------------------- |
| 00 b     | 不存在               |
| 01 b     | 位于请求者能力结构中 |
| 10 b     | 位于 MSI-X 表中      |
| 11 b     | 保留                 |

请求者能力寄存器在[26:16]位中列出了 ST 表中的条目数量。每个表条目宽度为 2 字节，在 TPH 能力寄存器集中实现的 ST 表如第 908 页图 20-18 所示，其中条目 0 被高亮显示。请求者能力寄存器还通过 3 个最低有效位描述了请求者支持哪些 ST 模式：

- 无 ST - 使用零作为 ST 位。在 TPH 请求者控制寄存器的 ST 模式选择字段中，当值为 时选择此模式。
- 中断向量 - 使用中断向量号作为表的偏移量，这意味着这些值包含在 MSI-X 表中。（ST 模式选择值为 。）
- 设备特定 - 使用设备特定的方法偏移到 TPH 能力结构中的 ST 表，因为 ST 值位于该处。这是推荐的实现方式，尽管特定请求与特定 ST 表项的关联方式不在本规范范围内。（ST 模式选择值为 010b。）
- 所有其他 ST 模式选择编码均保留供将来使用。

图 17：图 20-18：TPH 能力状态表

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-022.jpg)

## TLP 前缀

如果需要，可通过添加可选的 TLP 前缀来扩展转向标签位。当 TLP 附带一个或多个前缀时，报头通过设置格式字段中的最高有效位来报告该情况，如第 909 页图 20-19 所示。

## 第20章：规范修订版2.1的更新内容

图 18：图 20-19：TPH 前缀指示

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-023.jpg)

## IDO（基于 ID 的排序）

事务排序规则对于确保流量正常流动至关重要，但在某些情况下，这些规则并非必要，且可以改善延迟。特别是，来自不同请求者的 TLP 之间极不可能存在依赖关系，因此该功能允许软件启用这些 TLP 的重排序以提升性能。此操作的详细说明见第 301 页“基于 ID 的排序（IDO）”一节。

## ARI（替代路由 ID 解释）

此可选功能的动机是为了增加端点可用的功能编号数量。设备编号在 PCI 这样的共享总线架构中很有用，但在点对点架构中通常不需要。因此，规范编写者选择允许设备以不同方式解释 ID 路由命令的目标。实现方式是将设备编号始终定义为零，然后允许功能编号使用 ID 中原本属于设备编号的 5 个比特位。实际上，设备编号被取消，而功能编号扩展至 8 比特。使用 ARI 的 TLP 目标需要在软件使用此功能前被启用识别，但路径上的路由元件无需感知此变化，它们仅通过总线编号来确定路由。

## 电源管理改进

以下四项改进增强了系统有效管理电源的能力，这些内容均在第703页第16章"电源管理"中详细说明。

## DPA（动态电源分配

一组新的扩展配置寄存器定义了 D0 以下的 32 个子状态。这使得软件能够轻松更改设备的电源状态，而无需承受直接进入 D1 设备电源状态所带来的延迟开销。欲了解更多信息，请参见第 714 页的“动态电源分配（DPA）”。

## LTR（延迟容忍度报告）

允许端点报告其能够容忍的请求响应延迟，使系统软件能够更好地选择系统响应时间和睡眠状态。欲了解更多信息，请参见第 784 页的“LTR（延迟容忍度报告）”。

## OBFF（优化缓冲区刷新与填充）

类似地，允许系统报告端点应该或不应该发起 DMA 或中断流量的首选时间段，有助于协调系统睡眠时间并改善电源管理。欲了解更多信息，请参见第 776 页的“OBFF（优化缓冲区刷新与填充）”。

## ASPM 选项

这一变更仅允许设备在需要时选择不支持 ASPM 链路电源管理。在之前的规范版本中，对 L0s 的支持是强制性的，但现在变为可选。

## 第20章：规范修订版2.1的更新

## 配置改进

新增了少量配置寄存器，以提升软件对设备的可见性和控制能力。

## 内部错误报告

这旨在为交换机等没有驱动程序处理内部问题的设备提供一种标准化的内部问题报告方式。同时，它还增加了在错误发生时跟踪多个 TLP 报头的能力，而不再是像以前那样仅跟踪一个。该主题在错误章节中名为"内部错误"的部分（第 667 页）进行了讨论。

## 可调整大小的 BAR

这套新的扩展配置寄存器允许使用大量本地内存的设备报告它们是否能够使用较小的内存量，如果可以，哪些大小是可接受的。知道如何查找这些寄存器的软件可以找到如图20-20（第912页）所示的新寄存器，并根据系统内存与其他设备之间的竞争需求，对其进行编程以分配适合平台的内存大小。

使用这些寄存器需遵循以下规则：

1. 为避免混淆，仅当命令寄存器中的内存使能位被清除时，才应更改 BAR 的大小。
2. 规范强烈建议功能设备不要声明超出其有效使用范围的 BAR 大小。
3. 为确保最佳性能，软件应分配系统可支持的最大 BAR 大小。

图 19：图 20-20：可调整大小的 BAR 寄存器

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-026.jpg)

## 能力寄存器

该寄存器仅用于报告哪些 BAR 大小适用于此功能。位 4 至 23 用于此目的，其值如下所示：

- 位 4 - 1MB BAR 大小适用于此功能
- 位 
- 位 
- ……
- 位 将适用于此函数

图 20：图 20-21：可调整大小 BAR 能力寄存器

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-026-1778244554263-2.jpg)

## 控制寄存器

该寄存器中的 BAR 索引字段指示此大小所对应的 BAR（可能值为 0 至 5）。可调整大小 BAR 数量字段仅针对控制寄存器定义。

## 第20章：规范修订版2.1的更新内容

寄存器零为所有其他寄存器保留。它指示六个可能的 BAR 中有多少个实际具有可调整的大小。最后，BAR 大小字段由软件编程，以指定 BAR 索引字段（ ）所指示的 BAR 所需的大小。

图 21：图 20-22：可调整大小的 BAR 控制寄存器

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-027.jpg)

一旦可调整大小的值被编程，枚举软件将能够像往常一样工作：向每个 BAR 写入全 1 并读回，将报告所选择的大小。请注意，如果大小值发生更改，BAR 的内容将丢失，并且如果之前已设置，则需要重新编程。第 914 页的图 20-23 突出显示了类型 0 标头的配置标头空间中的 BAR 寄存器。

图 22：图 20-23：类型 0 配置标头中的 BAR

![img](img/20%20%E8%A7%84%E8%8C%83%202.1%20%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0/23385173-91a5-413b-a78f-0de460a0c229-028.jpg)

## 简化排序表

此项变更通过减少表中的条目数量来简化事务排序表。本质上，它不再区分读取操作的完成与未提交写入操作的完成。此举旨在减少需要测试的案例数量。更多详情，请参见第288页的“简化排序规则表”一节。