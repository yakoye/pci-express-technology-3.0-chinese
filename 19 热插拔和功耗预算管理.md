# 19 热插拔与功耗预算管理 

## 前一章

前一章描述了 PCIe 定义的三种复位类型：基本复位（包括冷复位和热复位）、热复位以及功能级复位（FLR）。讨论了使用边带复位信号 PERST#生成系统复位的方式，同时也介绍了基于带内 TS1 的热复位机制。

## 本章

本章描述了 PCI Express 热插拔模型。同时为所有支持热插拔功能的设备和外形规格定义了标准使用模型。对于热插拔卡而言，电源问题同样重要——在系统运行期间添加新卡时，必须确保其功耗需求不超过系统可提供的电力。因此需要一种机制，在允许设备运行前查询其功耗需求。功耗预算寄存器正是为此而设。

## 下一章

下一章将介绍 2.1 版规范新增的变更与特性。其中部分主题（如电源管理相关内容）已在前面章节中阐述，但其他内容则没有更合适的章节安排。最终，将这些内容集中归入一章，既能确保全面覆盖，也有助于明确哪些是新特性。

## 背景

采用 PCIe 的某些系统需要高可用性或不间断运行。在线服务供应商要求计算机系统每年仅出现几分钟或更短的停机时间。构建此类系统涉及多个方面，但设备可靠性显然至关重要。为实现这些目标，PCIe 支持针对插卡的热插拔/热替换解决方案，该方案提供三项重要能力：

1. 一种无需关闭系统即可更换故障扩展卡的方法
2. 在维修期间保持操作系统及其他服务持续运行
3. 关闭并重启与故障设备相关的软件

在 PCI 被广泛接受之前，许多专有的热插拔解决方案已被开发出来，以支持这种扩展卡的移除和更换。最初的 PCI 实现并不支持热移除和插入卡，但后来开发了两种标准化解决方案来支持 PCI 中的这一功能。第一种是用于 PC 服务器主板和扩展机箱实现的热插拔 PCI 卡。另一种称为热交换，用于基于无源 PCI 背板实现的 CompactPCI 系统。

在这两种解决方案中，控制逻辑用于将卡逻辑与共享 PCI 总线进行电气隔离。电源、复位和时钟受到控制，以确保卡在移除和更换时有序断电和上电，状态和电源 LED 指示灯会告知用户何时可以安全更换卡。

将热插拔支持扩展到 PCI Express 卡是显而易见的一步，设计者已将一些热插拔功能作为 PCIe 的“原生”特性。规范定义了配置寄存器、热插拔消息以及支持热插拔解决方案的过程。

## PCI Express 环境中的热插拔

PCIe 热插拔源自 PCI 标准热插拔控制器规范 1.0 版（SHPC 1.0）。PCI Express 热插拔的目标是：

- 支持与标准热插拔控制器规范定义的相同"标准化使用模型"。这确保从用户角度来看，PCI Express 热插拔与基于 SHPC 1.0 规范的现有实现完全一致

管理

- 支持现有操作系统实现的相同软件模型。然而，使用 SHPC 1.0 兼容驱动程序的操作系统无法与 PCI Express 热插拔控制器配合使用，因为后者具有不同的编程接口。

支持热插拔控制器所需的寄存器集成在各个根端口和交换端口中。在热插拔软件控制下，这些控制器及关联端口接口必须控制卡接口信号，以确保在更换卡时有序完成断电和上电操作。为此，它们需要：

- 对 PCI Express 卡连接器置位或释放 PERST#信号
- 对卡连接器切断或施加电源
- 选择性地开启或关闭与特定卡连接器关联的电源指示灯和注意指示灯，以引导用户关注该连接器，并指示插槽是否已供电。
- 监控插槽事件（例如卡移除）并通过中断向软件报告。

PCI Express 热插拔（类似于 PCI）采用“无意外”热插拔方法。换言之，用户通常不允许在未通知系统的情况下安装或移除 PCI Express 卡。软件随后会同时准备卡和插槽，最终向操作员指示热插拔过程的状态，并通知现在可以执行安装或移除操作。

## 意外移除通知

符合 PCIe 卡机电规范（CEM）的卡在连接器上实现了卡存在检测引脚（PRSNT1#和 PRSNT2#）。这些引脚比其他引脚更短，因此当卡从插槽中移除时，它们会首先断开接触。这可用于向软件提前发出“意外”移除通知，从而在信号断开接触前留出时间移除电源。

## PCI 与 PCIe 热插拔的区别

支持热插拔所需的元素在 PCI 和 PCIe 热插拔方案中基本相同。第 850 页图 19-1 展示了支持热插拔所需的 PCI 硬件和软件组件。PCI 方案在主板上实现了一个标准化的单一热插拔控制器，用于管理总线上的所有热插拔插槽。
在 PCI 环境中，需要隔离逻辑来在更改前将卡与共享总线电气断开，以避免在活动总线上产生信号毛刺。

PCIe 采用点对点连接（见第 851 页图 19-2），无需隔离逻辑，但每个连接器所连接的端口都需要独立的热插拔控制器。每个根端口和交换端口定义了标准化的软件接口，用于控制热插拔操作。

图 1：图 19-1：PCI 热插拔元件

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-04.jpg)



图 2：图 19-2：PCI Express 热插拔元件

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-05.jpg)

## 支持热插拔所需的元件

如图 19-2（第 851 页）所示，实现热插拔环境涉及多个组成部分。为便于讨论，我们将其分为软件和硬件要素。

## 软件要素

下表描述了支持热插拔功能的主要软件要素。

表 1：表 19-1：主要热插拔软件要素简介

| 软件元素                 | 提供方                   | 描述                                                         |
| ------------------------ | ------------------------ | ------------------------------------------------------------ |
| 用户界面                 | 操作系统供应商           | 操作系统提供的实用程序，允许用户请求关闭连接器电源以移除卡，或开启电源以使用刚安装的卡。 |
| 热插拔服务               | 操作系统供应商           | 处理操作系统发出的请求（称为热插拔原语）的服务。这些请求包括：- 提供插槽标识符 - 开启或关闭板卡电源 - 开启或关闭注意力指示灯 - 读取插槽当前电源状态（开启或关闭）。热插拔服务通过与热插拔系统驱动程序交互来满足这些请求。与热插拔系统驱动程序的接口（即 API）由操作系统供应商定义。 |
| 标准化热插拔系统驱动程序 | 系统主板供应商或操作系统 | 接收操作系统内热插拔服务发出的请求（热插拔原语）。与硬件热插拔控制器交互以完成请求。 |



表 2：表 19-1：主要热插拔软件元素介绍（续）

| 软件元素     | 提供方       | 描述                                                         |
| ------------ | ------------ | ------------------------------------------------------------ |
| 设备驱动程序 | 适配卡供应商 | 支持热插拔功能的设备驱动程序必须包含某些热插拔专用能力，包括：- 支持静默命令（Quiesce command）- 可选支持暂停命令（Pause command）- 支持启动命令（Start command）或可选恢复命令（Resume command） |

支持热插拔的系统可能使用不支持热插拔功能的操作系统。在这种情况下，尽管系统 BIOS 包含热插拔相关软件，但热插拔服务将不会存在。假设用户不尝试热插入或移除板卡，系统将作为标准的非热插拔系统运行：

- 系统启动固件必须确保所有注意指示灯处于关闭状态。
- 规范还指出：“热插拔插槽必须处于适合加载非热插拔系统软件的状态。”

## 硬件要素

表 19-2（第 853 页）列出了支持 PCI Express 热插拔操作所需的主要硬件元件。

表 3：表 19-2：主要热插拔硬件元件

| 硬件元件     | 说明                                                         |
| ------------ | ------------------------------------------------------------ |
| 热插拔控制器 | 接收并处理由热插拔系统驱动程序发出的命令。每个支持热插拔操作的根端口或交换端口均关联一个控制器。PCIe 规范为热插拔控制器定义了标准的软件接口。 |



表 4：表 19-2：主要热插拔硬件元件（续）

| 硬件元件         | 描述                                                         |
| ---------------- | ------------------------------------------------------------ |
| 插槽电源切换逻辑 | 允许在程序控制下打开或关闭插槽的电源。由热插拔控制器在热插拔系统驱动程序的指示下进行控制。 |
| 卡复位逻辑       | 热插拔控制器根据热插拔系统驱动程序的指令，驱动特定插槽的 PERST#信号。 |
| 电源指示灯       | 指示连接器当前是否处于供电状态。由每个端口关联的热插拔逻辑控制，并受热插拔系统驱动程序指令支配。 |
| 注意指示灯       | 用于引起操作员对需要维护的连接器的注意。由热插拔逻辑控制，并由热插拔系统驱动程序进行指示。 |
| 注意按钮         | 由操作员按下，以通知热插拔软件有关更换插卡请求的信息。       |
| 卡存在检测引脚   | 共有两个这样的引脚：PRSNT1#位于卡槽的一端，PRSNT2#位于另一端。这些引脚比其他引脚短，因此在拔出卡时会先断开连接。系统主板将 PRSNT1#接地，并将 PRSNT2#通过上拉电阻连接到热插拔控制器的输入端。对于更宽的连接器，还定义了额外的 PRSNT2#引脚，以支持将较短的卡插入较长的连接器并进行识别。卡本身将 PRSNT1#与 PRSNT2#短接，因此如果卡未物理插入，PRSNT2#输入为高电平；如果已插入，则为低电平。 |

## 卡的移除与插入流程

以下关于典型插拔卡的描述旨在作为入门介绍。需注意，后续章节所述流程假设由操作系统（而非热插拔系统驱动程序）负责配置新安装的设备。若热插拔系统驱动程序承担此职责，热插拔服务将调用该驱动程序并指示其配置新安装的设备。

## 开启与关闭状态

处于开启状态的插槽具有以下特征：

- 插槽已通电。
- REFCLK 已开启。
- 链路处于活动状态或活动状态电源管理状态。
- PERST# 信号已解除断言。

处于关闭状态的插槽具有以下特征：

- 插槽电源已关闭。
- 参考时钟（REFCLK）已关闭。
- 链路处于非活动状态。（交换端口根部的驱动器处于高阻态）
- PERST#信号已置位。

## 关闭插槽

关闭当前处于开启状态的插槽所需步骤：

1. 停用链路。这可能涉及发送 EIOS 以进入高阻态。
2. 向插槽置位 PERST#信号。
3. 关闭插槽的 REFCLK。
4. 移除插槽的电源。

## 开启插槽

开启当前处于关闭状态的插槽的步骤：

1. 对插槽施加电源。
2. 向插槽提供 REFCLK 时钟信号。
3. 解除插槽的 PERST#信号。系统必须满足相对于 PERST#上升沿的建立时间和保持时间要求（具体规定见 PCI Express 规范）。

在电源和时钟恢复且 PERST#信号撤销后，两端端口的物理层将执行链路训练与初始化。当链路激活时，设备将初始化 VC0（包括流量控制），使链路准备好传输 TLP 数据包。

## 卡片移除流程

当需要移除卡片时，需执行一系列步骤，以便为安全移除卡片做好软件和硬件准备，并设置正在处理卡片的指示灯状态。正常操作期间指示灯的状态如下：

- 注意指示灯（琥珀色或黄色）——正常操作期间为“熄灭”。
- 电源指示灯（绿色）——正常操作期间为“点亮”。

软件通过配置写入操作向热插拔控制器发送请求，这些写入操作针对支持热插拔端口的插槽控制寄存器。这些寄存器控制插槽的供电以及指示灯的状态。

事件序列如下：

1. 操作员通过按下插槽的注意按钮，或使用系统用户界面选择待移除卡的物理插槽编号来请求移除卡。如果使用了按钮，热插拔控制器会检测到此事件并向根复合体发送中断。该中断指示热插拔服务调用热插拔系统驱动程序读取插槽状态信息，并检测注意按钮请求。
2. 接下来，热插拔服务命令热插拔系统驱动程序将插槽的电源指示灯闪烁 5 秒，作为对操作员的视觉反馈。如果此操作是通过按下注意按钮发起的，操作员可以在 5 秒间隔内再次按下按钮以取消请求。
3. 电源指示灯持续闪烁，同时热插拔软件正在验证该请求。如果该卡当前正用于某些关键系统操作，软件可能会拒绝该请求。在这种情况下，软件将向热插拔控制器发出命令，重新点亮电源指示灯。规范还建议软件通知操作员，例如通过消息或记录日志条目，说明请求被拒绝的原因。



1. 如果请求通过验证，热插拔服务实用程序将命令该卡的设备驱动程序使其进入静默状态。即，禁用其生成新请求的能力，并完成或终止所有未完成的根端口或交换端口请求。
2. 随后，软件通过根端口或交换端口（插槽所连接的端口）中的链路控制寄存器发出命令，禁用该卡的链路。
3. 接下来，软件命令热插拔控制器关闭插槽。
4. 成功断电后，软件发出电源指示灯关闭请求，关闭电源指示灯，以便操作员知道可以移除该卡。
5. 操作员松开机械固定锁扣（如有），使热插拔控制器从插槽中移除所有切换信号（例如 SMBus 和 JTAG 信号）。此时即可移除该卡。
6. 操作系统解除分配给该设备的内存空间、IO 空间、中断线等资源，并将这些资源释放以供将来分配给其他设备使用。

## 插卡流程

安装新卡的步骤基本与拔卡流程相反。以下步骤假设插槽在拔卡后保持原有状态（即电源指示灯处于关闭状态，表示插槽已准备好插入新卡）。

插入并启用新卡的步骤如下：

1. 操作员安装新卡并锁紧 MRL（维护请求锁）。若已实现 MRL 传感器功能，该传感器将向热插拔控制器发出锁扣已闭合的信号，从而接通切换的辅助信号并将 连接至插槽。
2. 接下来，操作员通过按下注意按钮或使用热插拔实用程序选择插槽，通知热插拔服务卡已安装。
3. 如果按钮被按下，它会向热插拔控制器发送事件信号，导致状态寄存器位被设置，并向根复合体发送系统中断。随后，热插拔软件从端口读取插槽状态并识别该请求。
4. 热插拔服务向热插拔系统驱动程序发出请求，命令热插拔控制器闪烁插槽的电源指示灯，以告知操作员不得移除该卡。从指示灯开始闪烁起，操作员有 5 秒的中止间隔，可通过再次按下按钮来中止该请求。
5. 电源指示灯在热插拔软件验证请求期间持续闪烁。请注意，软件可能无法验证该请求（例如，安全策略设置可能禁止启用该插槽）。若请求未通过验证，软件将向热插拔控制器发送指令，将电源指示灯重新关闭。规范建议软件通过消息或日志条目通知操作员，说明请求被拒绝的原因。
6. 热插拔服务向热插拔系统驱动程序发出请求，命令热插拔控制器开启该插槽。
7. 电源接通后，软件发送指令将电源指示灯点亮。
8. 链路训练完成后，操作系统命令平台配置例程通过分配必要资源来配置板卡功能。
9. 操作系统（利用供应商 ID 和设备 ID、类别代码、子系统供应商 ID 及子系统 ID 配置寄存器的值作为搜索条件）定位 PCI Express 设备中各功能对应的驱动程序，并将其加载到内存中。
10. 随后，操作系统调用驱动程序的初始化代码入口点，使处理器执行该初始化代码。此代码完成设备的配置工作，并在设备的 PCI 配置命令寄存器中设置相应位以启用设备。

## 标准化使用模型

## 背景

基于原始 1.0 版 PCI 热插拔规范构建的系统，其硬件和软件设计差异极大，因为该规范未定义标准化的寄存器或用户接口。因此，从不同供应商处购买支持热插拔系统的客户，会面临用户接口差异巨大的问题，当采购新系统时需要对操作员进行重新培训。此外，每位板卡设计人员都必须编写软件来管理其特定实现的热插拔控制器。PCI 热插拔控制器（HPC）规范的 1.1 修订版定义了：

- 标准用户接口，消除了操作员重新培训的需求
- 热插拔控制器的标准编程接口，使得标准化的热插拔驱动程序能够集成到操作系统中。PCI Express 实现了 HPC 规范未定义的寄存器，



因此，PCI 与 PCI Express 标准热插拔控制器驱动的实现略有不同。

## 标准用户界面

用户界面包含以下功能：

- 注意指示灯 - 通过 LED 的亮、灭或闪烁状态显示插槽的注意状态。规范定义闪烁频率为 1 至 2 赫兹，占空比为 50%（±5%）。该指示灯的状态完全由软件控制。
- 电源指示灯（在 PCI HP 1.1 中称为插槽状态指示灯）——显示插槽的电源状态，可处于亮起、熄灭或闪烁状态（频率为 1 至 2Hz，占空比为 ）。该指示灯由软件控制，但规范允许在硬件电源故障情况下存在例外。
- 手动操作固定锁扣及可选传感器——将卡固定在插槽内，并在锁扣松开时通知系统。
- 机电互锁装置（可选）——锁定卡或固定锁扣，防止在通电状态下拔出卡。
- 软件用户界面——允许操作员请求热插拔操作。
- 注意按钮 - 允许操作员手动请求热插拔操作。
- 插槽编号标识 - 提供板上插槽的视觉识别。

## 注意指示灯

如前文所述，规范要求系统供应商为每个热插拔插槽配备注意指示灯。该指示灯必须位于对应插槽附近，颜色为黄色或琥珀色。此指示灯用于提醒最终用户该插槽需要维护。规范明确区分了操作错误和验证错误，且不允许注意指示灯报告验证错误。验证错误是在开始热插拔操作前由软件检测并报告的问题。注意指示灯的行为详见第 860 页表 19-3。

表 5：表 19-3：插槽注意力指示器的行为与含义

| 指示器行为 | 注意力状态                                                   |
| ---------- | ------------------------------------------------------------ |
| 关闭       | 正常 - 正常运行                                              |
| 开启       | 注意 - 热插拔操作因操作问题失败（例如：外部线缆问题、附加卡问题、软件驱动程序问题及电源故障） |
| 闪烁       | 定位 - 根据操作员请求正在识别插槽                            |

## 电源指示灯

电源指示灯仅反映插槽主电源的状态，由热插拔软件控制。该指示灯为绿色，当插槽电源"开启"时点亮。

规范明确禁止根端口或交换端口硬件因电源故障或其他事件而自主更改电源指示灯状态。此规则仅有一个例外：允许平台检测电源卡滞故障。卡滞故障是指移除插槽电源的指令无效的情况。若系统设计为可检测此状态，则系统可覆盖根端口或交换端口关闭电源指示灯的指令，强制其保持点亮状态，从而通知操作员不应从插槽中移除该卡。规范进一步指出，支持卡滞故障检测为可选功能，若通过系统软件处理，"平台供应商必须确保标准使用模型的这一可选功能通过其他软件、平台文档或其他方式予以说明"。

电源指示灯的行为及相关电源状态列于第 861 页的表 19-4 中。请注意，在保持锁存器释放或通过 Prsnt1#和 Prsnt2#信号检测到卡被移除之前， 保持开启状态，且开关信号仍保持连接。



表 6：表 19-4：电源指示灯的行为与含义

| 指示灯行为 | 电源状态                                                     |
| ---------- | ------------------------------------------------------------ |
| 关闭       | 电源关闭 - 此时可以安全地移除或插入卡。所有电源已按照热插拔操作要求移除。仅当手动固定锁扣释放时，Vaux 电源才会被切断。 |
| 开启       | 上电 - 不允许移除或插入卡。当前插槽已通电。                  |
| 闪烁       | 电源转换 - 不允许移除或插入卡。此状态通知操作员，软件正在响应热插拔请求，当前正在移除或施加插槽电源。 |

## 手动操作固定锁扣与传感器

手动保持闩锁（MRL）是必需的，用于将 PCI Express 卡牢固固定在插槽中。每个 MRL 可配备一个可选传感器，用于向热插拔控制器报告闩锁已关闭或打开。规范也允许使用单个闩锁同时固定多张卡，但此类实现不支持 MRL 传感器。

MRL 传感器是一种开关、光学设备或其他类型的传感器，用于报告闩锁处于关闭还是打开状态。若检测到意外的闩锁释放，端口会自动禁用插槽并通知系统软件，但禁止自主更改电源或注意指示灯的状态。

当 MRL 传感器指示闩锁打开时，必须自动从插槽移除交换信号和辅助电源（Vaux）；当 MRL 传感器指示闩锁关闭时，必须恢复这些信号和电源。交换信号包括 和 SMBDAT。

该规范还描述了在没有 MRL 传感器时，移除 和 SMBus 电源的替代方法。PRSNT#2 引脚用于指示插槽中是否实际安装了板卡，并可触发端口移除已切换的信号。

## 机电互锁（可选）

可选的机电板卡互锁机制提供了一种更复杂的方法，确保在插槽通电期间板卡不会被移除。规范未定义互锁的具体性质，但指出它可以将附加卡或 MRL 物理锁定在适当位置。

锁定机制通过软件控制，但未定义具体的编程接口。相反，互锁由与插槽主电源使能相同的端口信号控制。

## 软件用户界面

操作员可通过软件界面请求移除或插入板卡。该界面由系统软件提供，同时系统软件还负责监控插槽并向操作员报告状态信息。规范明确指出，用户界面由操作系统实现，因此不在本规范范围之内。

操作员必须能够独立于其他插槽，对每个插槽发起操作。因此，操作员可以在某个插槽的热插拔操作进行过程中，通过软件用户界面或注意按钮对另一插槽发起热插拔操作。无论操作员使用何种界面启动第一个热插拔操作，此功能均可实现。

## 注意按钮

注意按钮是一个瞬时接触式按钮开关，位于对应热插拔插槽附近或模块上。操作员按下此按钮可启动该插槽的热插拔操作（例如移除或插入板卡）。按下注意按钮后，电源指示灯开始闪烁。从闪烁开始起，操作员有 5 秒时间通过再次按下按钮来中止热插拔操作。

规范建议，若由注意按钮启动的操作失败，系统软件应通知操作员该故障。例如，可报告或记录说明故障性质的提示信息。

## 插槽编号识别

软件和操作员必须能够根据插槽编号识别物理插槽。每个支持热插拔的端口必须实现供软件识别物理插槽编号的寄存器。这些寄存器包括物理插槽



机箱编号与机箱号。主控机箱始终标记为机箱 0。其他机箱的编号必须为非零值，并通过 PCI-to-PCI 桥接器的机箱编号寄存器进行分配。

## 标准热插拔控制器信号接口

第 864 页的图 19-3 更详细地展示了交换端口内部的逻辑结构，以及插槽与端口之间路由的信号。标准化热插拔控制器的重要性在于其统一的软件接口，使得设备驱动程序能够集成到操作系统中。

PCIe 规范与卡机电（CEM）规范共同定义了热插拔 PCI Express 所需的插槽信号和支持。以下是支持标准使用模型所需的基本和可选端口接口信号列表：

- PWRLED#（必需）——控制电源指示灯状态的端口输出信号
- ATNLED#（必需）——控制注意指示灯状态的端口输出信号
- PWREN（若实现参考时钟则为必需）——控制插槽主电源的端口输出信号
- REFCLKEN#（必需）——端口输出，用于控制向插槽提供参考时钟
- PERST#（必需）——端口输出，用于控制插槽处的 PERST#
- PRSNT1#（必需）——在连接器处接地
- PRSNT2#（必需）——端口输入，在系统板上上拉，用于指示插槽中是否存在卡
- PWRFLT#（必需）—— 端口输入，用于通知热插拔控制器外部逻辑检测到的电源故障状况
- AUXEN#（如果实现了 AUX 电源则为必需）—— 端口输出，用于在 MRL 打开和关闭时控制切换的 AUX 信号和 AUX 电源。当 AUX 电源存在时，MRL#信号是必需的。
- MRL#（如果实现了 MRL 传感器则为必需）—— 来自 MRL 传感器的端口输入
- BUTTON#（如果实现了注意按钮则为必需）—— 端口输入，指示操作员已按下注意按钮

图 3：图 19-3：交换机内的热插拔控制功能

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-18.jpg)

## 热插拔控制器编程接口

热插拔控制器的标准编程接口通过 PCI Express 能力寄存器块提供，如图 19-4（第 865 页）所示，其中热插拔相关寄存器已高亮标注。热插拔功能具有以下特性：



主要位于为根端口和交换端口定义的插槽寄存器中。设备能力寄存器在某些实现中也会被使用，本章后续将对此进行说明。

图 4：图 19-4：用于热插拔的 PCIe 能力寄存器

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-19.jpg)

## 插槽能力

第 866 页的图 19-5 展示了插槽能力寄存器及其位域。硬件初始化所有这些能力寄存器字段，以反映该端口实现的功能。此寄存器适用于卡槽和机架安装实现，但指示灯和注意按钮除外。软件必须读取模块内的设备能力寄存器，以确定是否实现了指示灯和注意按钮。第 866 页的表 19-5 列出并定义了插槽能力字段。

图 5：图 19-5：插槽能力寄存器

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-20.jpg)

表 7：表 19-5：插槽能力寄存器字段及描述

| Bit(s)位(比特) | Register Name and Description |
| -------------- | ------------------------------------------------------------ |
| 0              | Attention Button Present - indicates the presence of an attention button on the chassis adjacent to the slot. |
| 1              | Power Controller Present - indicates the presence of a power controller for this slot. |
| 2              | MRL Sensor Present - indicates the presence of a MRL Sensor on the slot. |
| 3              | Attention Indicator Present - indicates the presence of an attention indicator on the chassis adjacent to the slot. |
| 4              | Power Indicator Present - indicates the presence of a power indicator on the chassis adjacent to the slot. |

表 8：表 19-5：插槽能力寄存器字段及描述（续）

| 位    | 寄存器名称与描述                                             |
| ----- | ------------------------------------------------------------ |
| 5     | 热插拔突发（Hot-Plug Surprise）——表示用户可能在没有事先通知的情况下从系统中移除该卡。这告知操作系统允许此类移除操作，且不影响软件的持续运行。 |
| 6     | 支持热插拔（Hot-Plug Capable）——表示该插槽支持热插拔操作。   |
| 14:7  | 插槽功率限制值（Slot Power Limit Value）——指定该插槽可提供的最大功率。该限制值需乘以下一个字段中指定的比例系数。 |
| 16:15 | 插槽功率限制比例 - 指定插槽功率限制值的缩放因子。            |
| 17    | 机电互锁存在 - 表示该插槽已实现此功能。                      |
| 18    | 不支持命令完成 - 表示该插槽在命令完成时不会生成软件通知。早期版本执行热插拔命令有时需要较长时间（例如，通过 总线通信以开启或关闭电源可能需要一秒钟或更长时间），并在最终完成时产生中断。当此位被设置时，表示该端口可以无延迟地接受对插槽控制寄存器中所有字段的写入，因此无需通知。 |
| 31:19 | 物理插槽编号 - 表示与此端口关联的物理插槽编号。必须由硬件初始化为机箱内唯一的编号。请注意，软件需要此编号来将物理插槽与逻辑插槽 ID（该设备的总线、设备及功能编号）关联起来。 |

## 插槽功率限制控制

规范提供了一种方法，使软件能够限制安装在扩展插槽或背板实现中的卡所消耗的功率。支持此功能的寄存器包含在插槽能力寄存器中。

## 插槽控制

软件通过插槽控制寄存器（如图 19-6 第 868 页所示）管理热插拔事件。该寄存器允许软件启用各种热插拔功能并控制热插拔操作。它还用于启用中断生成，以及启用可能导致中断生成的热插拔事件源。

图 6：图 19-6：插槽控制寄存器

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-22.jpg)



表 9：表 19-6：插槽控制寄存器字段及描述

| 位   | 寄存器名称及描述                                             |
| ---- | ------------------------------------------------------------ |
| 0    | 注意力按钮按下使能。当设置此位时，若按下注意力按钮，将启用热插拔中断（如果已启用）或断言唤醒（Wake#）消息的生成。 |
| 1    | 电源故障检测使能。当设置此位时，检测到电源故障时将启用热插拔中断（如果已启用）或唤醒（Wake#）消息的生成。 |
| 2    | MRL 传感器变更使能。当设置此位时，检测到 MRL 传感器变更事件时将启用热插拔中断或唤醒（Wake#）消息（如果已启用）的生成。 |
| 3    | 存在检测变更使能。当设置此位时，允许在插槽状态寄存器中的存在检测变更位被置位时生成热插拔中断或唤醒消息。 |
| 4    | 命令完成中断使能。当设置此位时，允许生成热插拔中断，通知软件热插拔控制器已准备好接收下一条命令。 |
| 5    | 热插拔中断使能。当设置此位时，允许生成热插拔中断。           |
| 7:6  | 注意指示灯控制。对该字段的写入控制注意指示灯的状态，读取返回当前状态，具体如下：- 00b = 保留 - 亮 - 闪烁 - 灭 |
| 9:8  | 电源指示灯控制。对该字段的写入控制电源指示灯的状态，读取返回当前状态，具体如下：- 00b = 保留 - 常亮 - 闪烁 - 熄灭 |
| 10   | 电源控制器控制。对该字段的写入切换插槽主电源，读取返回当前状态： 电源开启， 电源关闭 |

表 10：表 19-6：插槽控制寄存器字段及描述（续）

| 位   | 寄存器名称与描述                                             |
| ---- | ------------------------------------------------------------ |
| 11   | 机电互锁控制 - 若实现了互锁功能，向该位写入 1b 可切换其状态，写入 0b 则无任何效果。读取该位始终返回 0b。 |
| 12   | 数据链路层状态变更使能 - 若数据链路层链路活动报告能力为 1b，设置该位可在数据链路层链路活动位发生变化时启用软件通知。若数据链路层链路活动报告能力为 0b，则该位变为只读且值为 0b。 |

## 插槽状态与事件管理

热插拔控制器监控多种事件，并将这些事件报告给热插拔系统驱动程序。软件可以使用"检测"位来确定发生了哪个事件，而状态位则标识变化的性质。软件必须清除已变化的位，以便检测后续变化。请注意，这些事件是否通过系统中断报告给系统，取决于插槽控制寄存器中相关的使能位。

图 7：图 19-7：插槽状态寄存器

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-24.jpg)



表 11：表 19-7：插槽状态寄存器字段及描述

| 位位置 | 寄存器名称与描述                                             |
| ------ | ------------------------------------------------------------ |
| 0      | 注意按钮已按下 - 如果实现了该按钮，则在按下注意按钮时设置此位。 |
| 1      | 电源故障检测 - 如果实现了支持电源故障检测的电源控制器，则当该控制器检测到本插槽出现电源故障时设置此位。规范指出，无论电源控制设置如何，也无论插槽是否被占用，电源故障均可能在任何时刻被检测到。 |
| 2      | MRL 传感器状态变更 - 若实现了 MRL 传感器，则在检测到 MRL 传感器状态变化时置位。若未安装传感器，此位始终为 0。 |
| 3      | 存在检测状态变更 - 当检测到存在检测状态位发生变化时置位。    |
| 4      | 命令完成 - 若插槽能力寄存器中的"不支持命令完成"位为 0b，则当热插拔命令执行完毕且热插拔控制器可接收新命令时置位。从技术角度而言，仅能保证最后一种含义：无论前一条命令是否实际完成，控制器已准备好接收新命令。 |
| 5      | MRL 传感器状态 - 置位时表示 MRL 传感器的当前状态（若已实现）：0b = MRL 关闭，1b = MRL 打开 |
| 6      | 存在检测状态——该位指示插槽中是否存在卡，所有实现插槽的下行端口都必须具备此功能。其值为物理层检测逻辑与为该插槽实现的任何其他边带检测机制（如 PRSNT1#和 PRSNT2#）的逻辑"或"结果。两者之间的主要区别在于：引脚无需电源即可物理检测到卡的存在，因此无需恢复供电即可报告状态；而使用物理层检测逻辑则需要供电。 |

表 12：表 19-7：插槽状态寄存器字段及描述（续）

| 位位置 | 寄存器名称及描述                                             |
| ------ | ------------------------------------------------------------ |
| 7      | 机电互锁状态 - 如果实现了机电互锁，该位指示其处于锁定状态（1b）或解锁状态（0b）。 |
| 8      | 数据链路状态已更改 - 当链路状态寄存器中的数据链路层链路活动位发生变化时，该位被置位。针对此事件，软件必须读取数据链路层链路活动位，以确定在向热插拔设备发送配置周期之前链路是否处于活动状态。 |

## 附加卡能力

如图 19-8（第 873 页）所示的设备能力寄存器中，也包含与附加卡相关的字段，用于记录热插拔控制器报告其插槽可用的功率。当以下任一情况发生时，必须通过 Set_Slot_Power_Limit 消息自动传达此信息：

- 对插槽能力寄存器的配置写入会更改插槽功率限制值和插槽功率限制比例值。
- 链路从非 DL_UP 状态转换为 DL_Up 状态（除非插槽能力寄存器尚未初始化）。

该消息使用消息中的值更新捕获的插槽功率限制值和比例寄存器，使该信息可供其设备驱动程序随时使用。



图 8：图 19-8：设备能力寄存器

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-27.jpg)

## 静默化卡与驱动程序

## 概述

在从系统中移除卡之前，必须完成两件事：设备驱动程序必须停止访问该卡，且该卡必须停止发起或响应新的请求。具体实现方式因操作系统而异，但必须满足以下条件：

- 操作系统必须停止向设备驱动程序发出新请求，或指示驱动程序停止接受新请求。
- 驱动程序必须终止或完成所有未完成的请求。
- 必须禁用该卡生成中断或请求的功能。

当操作系统命令驱动程序及其设备进入静默状态时，操作系统不得期望该设备仍保留在系统中（换言之，该设备可能被移除且不会用相同型号的卡替换）。

## 暂停驱动程序（可选）

可选地，操作系统可以实现“暂停”功能，以临时停止驱动程序活动，预期同一张卡将被重新插入。然而，如果卡未能在合理时间内重新安装，则必须将驱动程序静默并从内存中移除。

例如，当前已安装的卡出现故障，或正在被替换为更新版本以进行升级。如果希望从软件和操作角度来看该过程无缝衔接，驱动程序需要将设备静默，保存当前上下文（寄存器内容、本地微控制器的堆栈和指令指针等），并关闭插槽电源。随后可安装新卡并通电，待其上下文恢复后，即可从断点处恢复正常运行。当然，如果旧卡已发生故障，可能无法简单地恢复运行。

## 静默控制多个设备的驱动程序

如果驱动程序控制多张卡，且收到操作系统要求其停止与某张特定卡相关的活动时，它必须仅停止与该卡及该卡本身相关的活动。

## 停止故障卡的活动

如果某张卡发生故障，驱动程序可能无法完成先前向该卡发出的请求。在这种情况下，驱动程序必须检测到错误，终止未完成的请求，并尝试重置该卡。

## 原语

本节讨论热插拔软件元素及其之间传递的信息。有关软件元素及其相互关系的回顾，请参考第 852 页的表 19-1。操作系统内的热插拔服务与热插拔系统驱动程序之间的通信以请求形式进行。规范未定义这些请求的具体格式，但定义了基本请求类型及其内容。热插拔服务向热插拔系统驱动程序发出的每种请求类型被称为原语。这些原语在第 875 页的表 19-8 中列出并描述。



表 13：表 19-8：原语

| 原语                                                         | 参数                                                         | 描述                                                         |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 查询热插拔系统驱动程序                                       | 输入：无                                                     | 请求热插拔系统驱动程序返回其所控制插槽的一组逻辑插槽 ID。    |
| 返回：该驱动程序所控制插槽的逻辑插槽 ID 集合。               |                                                              |                                                              |
| 设置插槽状态                                                 | 输入参数：- 逻辑插槽 ID - 新的插槽状态（开启或关闭）。- 新的注意指示灯状态。- 新的电源指示灯状态。 | 此请求用于控制每个插槽及其关联的注意指示灯。请求成功完成时，将返回“状态更改成功”参数。如果在尝试更改状态过程中发生故障，热插拔系统驱动程序应返回相应的故障消息（参见中间列）。除非另有说明，否则应将卡保持在关闭状态。 |
| 返回：请求完成状态：- 状态更改成功 - 故障-频率错误 - 故障-电源不足 - 故障-配置资源不足 - 故障-电源故障 - 故障-一般性故障 |                                                              |                                                              |
| 查询插槽状态                                                 | 输入：逻辑插槽 ID                                            | 此请求返回指定插槽的状态（如果存在卡）。热插拔系统驱动程序必须返回插槽电源状态信息。 |
| 返回：- 插槽状态（开启或关闭） - 卡电源需求                  |                                                              |                                                              |

表 14：表 19-8：原语（续）

| 原语                   | 参数              | 描述                                                         |
| ---------------------- | ----------------- | ------------------------------------------------------------ |
| 插槽状态变更的异步通知 | 输入：逻辑插槽 ID | 这是热插拔系统驱动程序向热插拔服务发出的唯一原语（由规范定义）。当驱动程序检测到插槽状态发生非请求性变化时，便会发送该原语。例如运行时电源故障，或卡被安装到先前为空且无警告的插槽中。 |
| 返回值：无             |                   |                                                              |

## 电源预算简介

PCI Express 电源预算功能的主要目标是为系统运行时添加的 PCI Express 热插拔设备分配电源。这确保了系统能够为这些设备分配适当的电力和冷却资源。

规范指出：“对于不需要热插拔或集成在系统板上的 PCI Express 设备，电源预算能力是可选的。”截至本文撰写时发布的所有外形规格均未要求支持热插拔或电源预算能力，但这些规格经常变更。

系统电源预算始终需要支持所有系统板设备和附加卡。新能力提供了管理热插拔卡预算流程的机制。每种外形规格定义了特定扩展槽的最小和最大功耗。例如，CEM 规范限制了扩展卡在完全启用前可消耗的功率，但启用后，它可以消耗该插槽规定的最大功率。若缺乏电源预算能力寄存器，系统设计人员需负责确保电源预算正确分配，并保证有足够的散热能力来支持安装到连接器上的任何合规卡。

规范定义了支持电源预算管理的配置寄存器，但未定义电源预算管理的方法和流程。下一节将描述电源预算管理中涉及的硬件和软件要素，包括已定义的配置寄存器。



## 电源预算管理要素

图 19-10 展示了热插拔卡的电源预算管理概念。以下列出并描述了参与电源预算、分配和报告流程的每个要素的作用：

- 系统固件用于电源管理（启动时使用）。
- 电源预算管理器（运行时使用）。
- 扩展端口（连接卡槽的接口）。
- 附加设备（支持电源预算功能）。

## 系统固件

由平台设计人员针对特定系统编写，负责报告系统电源信息。规范建议将以下电源信息报告给 PCI Express 电源预算管理器，该管理器在运行时分配并验证功耗与散热能力：

- 系统可用总功率
- 固件分配给系统设备的功率
- 系统中插槽的数量和类型。

固件也可以为支持电源预算能力寄存器集的 PCIe 设备分配电源，例如在启动期间使用的热插拔设备。如图 19-9（第 878 页）所示的电源预算能力寄存器包含一个系统分配位，该位由硬件（通常由固件）初始化，用于通知电源预算管理器该设备的电源已包含在系统电源分配中。如果是这样，电源预算管理器仍需要读取并保存已分配电源的热插拔设备的电源信息，以防这些设备在运行时被移除。

表 15：图 19-9：电源预算寄存器

| 310                                 |                    |      |
| ----------------------------------- | ------------------ | ---- |
| PCIe 扩展能力头部                   |                    |      |
| RsvdP                               | 数据选择寄存器     | 04h  |
| 数据寄存器                          | 08h                |      |
| RsvdP                               | 电源预算能力寄存器 | 0Ch  |
| 系统分配位 电源预算能力寄存器的位 0 |                    |      |

## 电源预算管理器

该管理器在操作系统安装时初始化，并从系统固件接收功率预算信息，尽管规范未定义传递此信息的方法。该管理器负责为所有 PCI Express 设备分配功率，包括：

- 尚未由系统分配功率的 PCI Express 设备（包括支持功率预算的嵌入式设备）。
- 在启动时安装的热插拔设备。
- 在运行时新增的设备。

## 扩展端口

第 880 页的图 19-10 展示了一个热插拔端口，该端口必须在插槽能力寄存器中实现插槽功率限制和插槽功率等级字段。固件或功率预算管理器必须向这些字段加载一个值，该值代表此端口支持的最大功率。当软件写入这些字段时，端口会自动向设备发送一条设置插槽功率限制消息。当软件配置通过热插拔安装新增的板卡时，也会写入这些字段。



## 规范要求：

- 任何带有插槽的下行端口（其 PCIe 能力寄存器中的插槽实现位已设置）必须实现插槽能力寄存器。
- 软件必须初始化与附加插槽相连的下行端口的插槽能力寄存器中的插槽电源限制值和缩放比例字段。
- 上行端口必须实现设备能力寄存器。
- 当卡插入插槽且软件更新下行端口的电源限制值和缩放比例时，该端口将自动向已安装卡上的上行端口发送设置插槽电源限制消息。
- 消息接收方必须使用数据有效载荷来限制整个卡片的功耗，除非该卡片永远不会超过相应机电规范中规定的最低值。

## 附加设备

支持功耗预算功能的扩展卡必须在设备能力寄存器中包含插槽功率限制值和插槽限制比例字段，并配备用于报告功耗相关信息的功耗预算能力寄存器组。

这些设备消耗的功率不得超过外形规格规定的最低功率。在功耗预算软件通过设置插槽功率限制消息分配额外功率后，设备方可消耗指定功率，但必须在其完成配置并启用之后才能执行此操作。

设备驱动程序——设备的软件驱动程序负责在启用设备前，验证是否有足够的功率供设备正常运行。如果功率低于设备所需，设备驱动程序需将此情况上报给更高层级的软件管理模块。

图 9：图 19-10：功率预算涉及的元素

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-34.jpg)



## 插槽功率限制控制

软件负责确定扩展设备允许消耗的最大功率。此分配基于系统内的功率分配、散热能力等因素。系统功率和散热限制的信息来自系统固件。固件或电源管理器负责向每个扩展端口报告功率限制。

## 扩展端口提供插槽功率限制

软件通过写入插槽能力寄存器的"插槽功率限制值"和"插槽功率限制比例"字段，来指定设备可消耗的最大功率。软件必须指定符合规范定义的某个最大功率值。例如，CEM 规范 2.0 版本定义的功耗值如表 19-9 所示。

关于这些数值有一个值得注意的细节：标准高度的 x1 服务器卡在复位后仅限使用 10W 功率，只有在完成配置并启用后才允许使用完整的 25W。同样，x16 显卡在配置并启用前仅限使用 25W，之后才能使用完整的 75W。

表 16：表 19-9：系统板扩展插槽的最大功耗

|          | X1 链路           | X4/X8 链路          | X16 链路      |                         |                       |
| -------- | ----------------- | ------------------- | ------------- | ----------------------- | --------------------- |
| 标准高度 | 10 瓦（最大桌面） | 25 瓦（最大服务器） | 25 瓦（最大） | 25 瓦（最大服务器功耗） | 75 瓦（最大显卡功耗） |
| 半高卡   | 10 瓦（最大功耗） | 25W（最大）         | 25W（最大）   |                         |                       |

除了基础 CEM 规范外，针对高功耗设备还定义了两项额外规范。首先是 PCIe x16 Graphics 150W-ATX 规范 1.0，该规范定义了可从卡连接器获取 75W 功率、并从独立 3 针 ATX 电源连接器获取另外 75W 功率的显卡。其次是 PCIe High Power CEM 规范 1.0，该规范通过增加另一个 3 针电源连接器将功率提升至 225W，或采用 4 针 ATX 连接器使总功率达到 300W。

当电源预算软件写入插槽功率寄存器时，扩展端口会向扩展设备发送 Set_Slot_Power_Limit 消息。该过程如图 19-11（第 882 页）所示。

图 10：图 19-11：插槽功率限制序列

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-36.jpg)



## 扩展设备限制功耗

设备驱动程序从捕获的插槽功率限制和缩放字段中读取数值，以验证可用功率是否足以运行设备。可能存在以下几种情况：

- 可用功率足以使设备以全性能运行。此时，驱动程序通过向配置命令寄存器写入数据来启用设备，允许设备消耗不超过功率限制字段中指定上限的功率。
- 可用功率足以运行设备，但无法达到全性能。此时，驱动程序需配置设备，使其消耗的功率不超过功率限制字段中指定的上限。
- 可用功率不足以运行设备。此时，驱动程序不得启用该卡，并必须将功率不足的情况上报给上层软件层，由上层软件层通知最终用户该问题。
- 可用功率超过了外形规格规范指定的最大功率。这种情况不应发生，但如果发生，设备不得消耗超出外形规格允许的最大功率。
- 可用功率低于外形规格所规定的最低值。这违反了规范，规范指出扩展端口“不得发送指示低于插槽外形规格机电规范中规定的最低值的 Set_Slot_Power_Limit 消息。”

某些扩展设备消耗的功率可能低于其外形规格规定的最低限值。此类设备允许丢弃 Set_Slot_Power_Limit 消息中传递的信息。当读取插槽功率限制值和比例字段时，这些设备返回零值。

## 功率预算能力寄存器组

这些寄存器允许功率预算软件根据设备通过其功率预算数据选择和数据寄存器提供的信息更有效地分配功率。此功能类似于电源管理能力寄存器中的数据选择和数据字段。然而，功率预算寄存器向软件提供更详细的信息，以帮助其确定在运行时添加的扩展卡的影响。
系统功耗预算与散热需求。通过此功能，设备可报告其功耗情况：

- 各电源轨的功耗
- 不同电源管理状态下的功耗
- 不同运行工况下的功耗

对于集成在系统主板或不支持热插拔的扩展设备上的设备，这些寄存器并非必需。第 884 页的图 19-12 展示了电源预算能力寄存器组，并说明了用于访问电源预算信息的数据选择字段和数据字段。

电源预算信息存储在一个由多个 32 位条目组成的表中。每个表条目包含设备支持的不同工作模式下的电源预算信息。通过数据选择字段选择每个表条目，然后从数据字段中读取所选条目。索引值从零开始，并按顺序实现。当所选索引在数据字段中返回全零时，表示已到达电源预算表的末尾。第 885 页的图 19-13 展示了数据字段中可用信息的格式和类型。

表 17：图 19-12：电源预算能力寄存器

| 31 PCIe 扩展能力头部 |                    |      |
| -------------------- | ------------------ | ---- |
|                      |                    |      |
| RsvdP                | 数据选择寄存器     | 04h  |
| 数据寄存器           | 08h                |      |
| RsvdP                | 电源预算能力寄存器 | 0Ch  |



图 19-13：电源预算数据字段格式与定义

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-39.jpg)

![img](img/19%20%E7%83%AD%E6%8F%92%E6%8B%94%E5%92%8C%E5%8A%9F%E8%80%97%E9%A2%84%E7%AE%97%E7%AE%A1%E7%90%86/42392084-9c6a-4415-85bf-dc1850418fd6-39-1778244795726-1.jpg)

001b 辅助
010b 空闲
011b 持续
111b 最大
所有其他编码均为保留。
工作原理：条目 0。通过将索引值放入电源预算数据选择寄存器，然后读取电源预算数据中返回的值来读取每个条目。
此条目描述的操作条件的 PM 子状态：000b 默认子状态，001b-111b 设备特定子状态，所有其他编码均为保留。
此条目描述的 PM 状态：

```
00b D
01b D1
10b D2
11b D3
```



该功能的电源预算数据由从寄存器开始的 条目表组成。当数据寄存器返回全 0 值时，表示表格结束。
D3 冷态 PM 状态描述 = 11b，且类型字段中为辅助电源或 PME 辅助电源。
D3 热态 任何其他类型值。